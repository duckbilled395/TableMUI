import React from 'react';

import './Header.scss'

const Header: React.FC = () => {
    return (
        <header className='header'>
header
        </header>
    );
};

export default Header;